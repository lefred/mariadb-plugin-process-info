name: Build and package PROCESS_INFO plugin

on:
  push:
    branches:
      - main
    tags:
      - "v*"

  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    uses: lefred/mariadb-plugin-ci/.github/workflows/build-plugin.yml@v1
    with:
      plugin_name: process-info
      plugin_directory: process_info
      build_target: process_info
      plugin_library: process_info.so
      cmake_options: -DPLUGIN_DATE_EXTRA=DYNAMIC
      test_suite: process_info
      run_tests: ${{ startsWith(github.ref, 'refs/tags/v') }}
